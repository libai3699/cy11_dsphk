# 合并方案与深度分析（项目负责人视角）

> 目标项目：`D:\李峰荣\AI学习\cy11_dsphk-main\cy11_dsphk-main`（抖音获客运营后台，Vue Vben Admin + Go/Gin）
> 来源 A：`D:\李峰荣\知识库\marketingskills\落地实测\餐饮获客\官网和工作台-app`（zaotai-workbench）
> 来源 B：`D:\李峰荣\知识库\自媒体运营团队\WorkBuddy 工作区`（自媒体多智能体运营团队）

---

## 0. 结论先行
- 目标项目是**生产级**后台：已具备登录/JWT 鉴权、6 个 AI Agent（accountdiagnosis/benchmark/hotspottopic/copywriting/storyboard/review）、商家/套餐/选题/脚本/分镜/排期/复盘等后端能力。**文档滞后于代码**，一切以代码为准。
- 两个来源**不是可直搬的代码，而是领域资产**：
  - 来源 A：餐饮代运营工作台（Vue3 + Express），核心资产是 **7 个内容营销 Agent 的 spec+prompt+schema+template**（领域知识结晶）+ 工作台 UI 与设计令牌。
  - 来源 B：抖音多智能体运营团队脚手架，**knowledge 知识库 + templates 模板 + 8 阶段管线模型 + 看板概念**（设计完善、产出早期）。
- **核心策略：只搬内容/资产，不搬框架。** 两源与目标栈不同，其价值在"领域知识 + UI 范式"，应作为数据/配置/页面迁移进目标 Go+Vue 运行时。

---

## 1. 三方定位对照

| 维度 | 目标项目 cy11_dsphk-main | 来源 A 官网和工作台-app | 来源 B 自媒体运营团队工作区 |
|---|---|---|---|
| 形态 | 全栈生产应用 | 代运营工作台（SPA+Express） | 多智能体团队脚手架（md+scripts） |
| 前端 | Vue3 + Vben Admin + Antd + Pinia | Vue3 + Vite + Antd（暗色） | 无（仅 dashboard.html 看板） |
| 后端 | Go + Gin + GORM + MySQL | Express + tsx（文件落盘，无 DB） | 无（无构建/无服务） |
| AI | 6 Agent，StepFun  provider | 7 Agent，声明式 YAML 编排框架 | 27 技能包 + .mdc 规则（Cursor/WorkBuddy 运行时） |
| 最值钱资产 | 生产骨架 + 鉴权 + Agent 层 | 7 Agent 领域内容 + UI 范式 | 知识库/模板/管线模型/看板概念 |

---

## 2. 目标项目缺口（决定"完善"方向）
- **前后端不匹配**：前端 api 含 `lines/plans/orders/payments/quotes/stats/notices/logs/devices/discoveries/files` 等模块，**后端无对应 handler**（仅前端设想）。
- 真实数据源缺失（抖音/来客接入）；支付未实现；无定时任务/队列/Webhook；无测试/CI/Docker；登录密码写死 env。

---

## 3. 资产 → 目标映射

| 来源资产 | 类型 | 目标落点 | 方式 |
|---|---|---|---|
| B `knowledge/pain-point-library.md` | 领域内容 | Go `PainPoint` + Vue 页面 | 数据迁移 + 页面 |
| B `knowledge/case-library.md` | 领域内容 | Go `CaseStudy` | 数据迁移 |
| B `knowledge/account-profile.md` | 领域内容 | Go `AccountProfile` | 数据迁移 |
| B `knowledge/platform-rules.md` | 领域内容 | Go `PlatformRule` | 数据迁移 |
| B `knowledge/templates/`（5 类） | 模板 | Go `ContentTemplate` | 数据迁移 |
| B `dashboard.html` 管线/漏斗 | 概念 | Vue 运营中控页 | 参考重建 |
| B 8 阶段管线 | 模型 | Go 工作流状态机（后续） | 重写 |
| A `server/ai/{a1..a7}` spec+prompt+schema+template | Agent 领域资产 | `server/internal/agent/*` 的 prompt/schema | 内容迁移 |
| A `src/` UI + `styles/tokens.css` | UI 范式 | `apps/web-antd/src` 新增页面/组件 | 适配 Vben |
| A 客户 CRM（client） | 模型 | 对齐现有 merchants 模块 | 对齐 |

---

## 4. 合并原则（硬约束）
1. **以目标 Go+Vue 运行时为唯一真相源**；所有移植适配现有约定（GORM 模型 / service / router / Vben 页面 / api 层）。
2. **不搬框架**：来源 A 的 Node 编排框架、来源 B 的 `.mdc`/`.workbuddy` skills 不照搬，只取其领域内容重写为数据/页面逻辑。
3. **分阶段、每阶段一个垂直切片、一个子 agent、限定范围**，避免上下文爆炸、保证可验证。

---

## 5. 里程碑计划
- **M0** 三方深度分析 + 合并方案（本阶段）
- **M1** 后端：运营知识库模块（来源 B knowledge → Go model/service/router/seed）← **当前执行**
- **M2** 前端：运营知识库页面（Vue 页面 + api + 菜单）
- **M3** 后端：Agent 领域资产迁移（来源 A 7 Agent 的 prompt/schema/template → `server/internal/agent/*`）
- **M4** 前端：补齐前后端不匹配模块（lines/plans/orders/payments/quotes/stats… 按业务优先级）
- **M5** 前端：运营中控看板（参考 B `dashboard.html` 的管线 + 漏斗）
- **M6** 验证与联调（go build / vue-tsc / 环境联调）+ 写 overview

---

## 6. 子 agent 上下文管理规则（用户重点关切）
- 一 agent 一垂直切片、一层（后端 **或** 前端），不跨界。
- 只 READ 指定的源文件 + 指定的目标参考文件（给绝对路径清单），不漫游整个仓库。
- 报告 ≤400 字，聚焦交付物清单与假设。
- **禁止修改源目录**；只写工作区指定路径；不删不改无关文件。
- 不跑依赖缺失的 build；只产出现在约定下、可编译风格的正确代码。

---

## 7. 风险
- 源与目标栈不同 → 已决策"只搬内容"。
- 目标文档滞后 → 以代码为准。
- 构建需 MySQL/Env → 子 agent 不跑 build，由负责人做结构 QC + 后续环境联调。
- 范围巨大 → 必须里程碑交付，避免一次性大改导致不可验证。
