# M4 前后端对照分析与决策

> 里程碑 M4（原计划"补齐前后端不匹配模块"）——证据驱动的范围修正记录
> 日期：2026-08-13 ｜ 负责人：WorkBuddy 项目负责人

## 一、结论先行（重要修正）

原计划假设"存在大量前后端不匹配模块（lines/plans/orders/payments/quotes/stats…）需要补齐"。
**经过对全量路由、api 层、页面 import 的逐条核对，该假设被推翻**：

- 后端 `router.go` 已注册 **50+ 端点**，覆盖 merchants / packages / account-auths / account-diagnoses / benchmarks / topics / scripts / storyboards / shooting-tasks / schedules / reviews / agent-configs / workspace / knowledge 全部业务资源。
- 前端 **16 个业务页面**（33 个 vue 文件中除 12 个框架页与 5 个 knowledge 页外）的 api import **全部能解析到后端存在的端点**。
- 之前担心的 lines / orders / payments / quotes / notices / configs / discoveries / devices 等"缺口"，真相是：**页面文件名沿用了旧模板命名，但内部实际调用 content-production / benchmarks / account-auths / merchants 等已存在端点**；同名 api 文件（quotes.ts / payments.ts / devices.ts…）是**无任何页面引用的死代码**。

## 二、对照矩阵（页面 → api → 端点）

| 前端页面 | 文件 | 调用的 api | 后端端点 | 状态 |
|---|---|---|---|---|
| 工作台 | dashboard/WorkspacePage | workspace/merchants/plans/content-production/shooting-tasks | /workspace/overview /merchants /packages /schedules /shooting-tasks | ✅（修复前 getShootingTasks 从桶不可见） |
| 商家列表 | users/UserListPage | merchants | /merchants CRUD | ✅ |
| 商家推进流程 | users/MerchantProcessPage | merchants | /merchants/:id/workspace | ✅ |
| 账号授权 | users/UserDevicesPage | account-auths | /account-auths CRUD | ✅ |
| 账号诊断 | users/AccountDiagnosisPage | account-diagnoses + merchants + plans + account-auths | /account-diagnoses | ✅ |
| 跟进记录 | users/DurationLogPage | **无 api import** | （无对应端点） | ⚠️ 纯占位页 |
| 团购套餐 | plans/PlanListPage | plans（=packages） | /packages CRUD | ✅ |
| 分成订单 | plans/PlanOrdersPage | **无 api import** | （无对应端点） | ⚠️ 纯占位页 |
| 对标账号库 | lines/LineListPage | lines.ts（=benchmarks） | /benchmarks CRUD + /analyze + /benchmark-analyses | ✅ |
| 选题中心 | content/ContentNoticesPage | content-production + benchmarks + merchants | /topics + /topics/generate | ✅ |
| 文案脚本 | content/ContentQuotesPage | content-production + topics | /scripts + /scripts/generate | ✅ |
| 分镜脚本 | content/ContentDiscoveriesPage | content-production | /storyboards | ✅ |
| 发布排期 | content/ContentPaymentsPage | content-production | /schedules | ✅ |
| 系统规则 | content/ContentConfigsPage | content-production | /agent-configs | ✅ |
| 拍摄任务 | logs/LogUserPage | content-production + shooting-tasks | /shooting-tasks | ✅ |
| 数据复盘 | logs/LogAdminPage | content-production | /reviews | ✅ |
| 运营知识库 ×5 | knowledge/* | knowledge | /knowledge/*（25 端点） | ✅（M1+M2） |

## 三、发现的问题清单与处置

| # | 问题 | 严重度 | 处置 |
|---|---|---|---|
| 1 | `api/admin/index.ts` 漏导出 `shooting-tasks`，而 WorkspacePage 从桶 `import { getShootingTasks }` → 桶解析失败 | **编译错误** | ✅ 已修复（index.ts 补 `export * from './shooting-tasks'`，已核对全仓无其他桶导入未解析） |
| 2 | DurationLogPage（跟进记录）、PlanOrdersPage（分成订单）为纯占位页，无 api、无后端端点 | 低 | **决策：保持占位，不发明业务**（后端无对应数据模型，且来源 A/B 素材不含订单/跟进业务；强行补全=发明业务，超出合并本意）。文档标注，待主理人决定是否立项 |
| 3 | 死代码 api 文件：devices / discoveries / files / payments / quotes / notices / configs / orders / stats / users（部分在 index.ts 导出但无任何页面引用） | 低 | **决策：不删除**（保守，避免隐藏引用风险；属清理项，留待主理人确认后单独处理） |
| 4 | UI 库分模块差异：业务页 13/16 用 **element-plus**（依赖已装），M2 新并入的 knowledge 5 页用 **antd**。项目实际是"Vben antd 外壳 + element-plus 业务组件"混合 | 低 | **决策：保持分模块差异，不强行统一**（统一 13 个页面=大重构且有回归风险；knowledge 新模块跟随 antd 外壳亦可接受）。记录在案，如需统一由主理人立项 |

## 四、剩余工作（真实的高价值缺口）

1. **M3 接线（最大缺口）**：7 个餐饮获客 agent 已迁入 `server/internal/agent/*` 但**未接路由/handler**（M3 时刻意留到接线阶段）。需在 handler 层新增调用端点（仿现有 6 agent 的 generate/analyze 模式：如 `POST /api/admin/agents/:name/run` 或按域建专用端点），并注册到 router.go。这是下一阶段主活。
2. **M5 运营中控看板**：现有 WorkspacePage 已有基础总览，可参考来源 B `dashboard.html` 的管线/漏斗可视化增强（选题→脚本→分镜→拍摄→排期→复盘漏斗）。
3. **M6 联调验证**：需项目环境（Go+MySQL、pnpm install）编译运行验证，沙箱无工具链。

## 五、执行说明

- M4 探索因子 agent 频率限制（429，10:56 重置）由负责人直接采样式完成（Glob+Read+Grep，未全读仓库）。
- 唯一必须修复项（#1）为 1 行改动，负责人兜底直接修复并提交，不派子 agent。
- 子 agent 恢复后：M3 接线拆 2 片（handler 端点 ×7 agent 拆两半）、M5 看板 1 片，各限定范围、只读范本、报告 ≤400 字。
