# 部署指南（抖音获客运营后台 cy11_dsphk）

> 适用对象：本项目（Vue Vben Admin 前端 + Go/Gin 后端）。
> 目标环境：**Linux 服务器 + 宝塔面板 + Nginx + MySQL**。
> 对外域名：`dspadmin.wangwei.tech`（前端）、`dspapi.wangwei.tech`（后端 API）。
> 本指南所有路径、端口、变量均来自仓库代码，可直接照做。

---

## 0. 架构总览

```
浏览器
 ├─ dspadmin.wangwei.tech  → Nginx 静态托管 dist（前端）
 └─ dspapi.wangwei.tech    → Nginx 反代 → 127.0.0.1:8989（Go 后端）→ MySQL
```

| 项 | 值 | 来源 |
|---|---|---|
| 后端监听端口 | `8989` | `server/internal/config/config.go` |
| 后端路由前缀 | `/api/admin` | `server/internal/router/router.go` |
| 前端构建命令 | `pnpm build` | `admin/package.json` |
| 前端产物目录 | `admin/apps/web-antd/dist` | Vite 默认输出 |
| 前端 API 地址 | `https://dspapi.wangwei.tech/api/admin` | `admin/src/configs/env/prod.js` |
| 后端构建（交叉编译） | `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o app ./cmd/main.go` | `server/README.md` |

> 关键点：前端请求路径 `/api/admin/xxx` 与后端路由前缀 `/api/admin` 完全对应，因此 Nginx 反代**原样转发即可，不改写路径**。

---

## 1. 前置条件（部署前确认）

1. 一台 Linux 服务器，已安装**宝塔面板**；
2. 宝塔已安装 **Nginx** 与 **MySQL**；
3. 已在宝塔 MySQL 中创建数据库 `cy11_dsphk`（字符集 utf8mb4）；
4. 域名 `dspadmin.wangwei.tech`、`dspapi.wangwei.tech` 已解析到该服务器 IP；
5. 已申请 **StepFun API Key**（`STEP_API_KEY`），AI 能力依赖它；
6. 本地具备 Node（^20.19 / ^22.18 / ^24）与 pnpm（>=10）用于构建前端（也可在服务器构建）。

---

## 2. 后端部署（Go）

### 2.1 交叉编译（在本地 Windows PowerShell 执行）
```powershell
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -o app ./cmd/main.go
```
产物为 `app`（Linux amd64 二进制）。

### 2.2 上传与目录
1. 在服务器建目录，例如 `/www/wwwroot/dspapi/`；
2. 上传编译好的 `app` 到该目录。

### 2.3 配置 `.env`
将仓库 `server/.env.example` 复制为同目录 `.env`，并按生产环境修改：

```env
# 基础
GIN_MODE=release                 # 生产必须为 release
SERVER_PORT=8989

# 数据库（填宝塔 MySQL 的实际账号）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的强密码
DB_NAME=cy11_dsphk

# JWT（务必改成随机长串，不要留占位值）
JWT_SECRET=请用 openssl rand -hex 32 生成
JWT_EXPIRES_HOURS=168

# 管理员（务必改强口令）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=你的强口令
ADMIN_REAL_NAME=超级管理员

# StepFun（AI 能力，填真实 key）
STEP_BASE_URL=https://api.stepfun.com/step_plan/v1
STEP_MODEL=step-3.7-flash
STEP_TIMEOUT_SECONDS=120
STEP_API_KEY=真实key
```
> 后端启动时会自动 `MigrateAndSeed` + `SeedKnowledge`，无需手动建表/灌数据。

### 2.4 启动（建议进程守护）
最简方式：
```bash
cd /www/wwwroot/dspapi && nohup ./app > app.log 2>&1 &
```
**更稳**：在宝塔「Supervisor」中添加守护进程，命令 `/www/wwwroot/dspapi/app`，崩溃自动重启、开机自启。

### 2.5 端口安全
后端 `r.Run(":"+Port)` 监听 `0.0.0.0:8989`。**不要对公网放行 8989**（在云安全组/防火墙关闭 8989 入站），仅允许本机 Nginx 反代访问即可。

---

## 3. 前端部署（Vben Admin）

### 3.1 构建（本地）
```bash
cd admin
pnpm install
pnpm build                    # 等价于 build:antd
```
产物位于 `admin/apps/web-antd/dist`。

### 3.2 上传与建站
1. 上传 `dist/` 整个目录到服务器，例如 `/www/wwwroot/dspadmin/`；
2. 宝塔建站 `dspadmin.wangwei.tech`，网站根目录指向 `/www/wwwroot/dspadmin/dist`；
3. 申请并部署 SSL（宝塔一键 HTTPS）。

### 3.3 Nginx 静态配置（SPA 兜底）
在站点 Nginx 配置中加入：
```
location / {
    try_files $uri $uri/ /index.html;
}
```
> 因为前端是单页应用，刷新子路由需回退到 `index.html`。

---

## 4. Nginx 反代（dspapi，核心步骤）

宝塔建站 `dspapi.wangwei.tech`（纯 API 反代，无需网站目录），Nginx 配置：
```
location /api/admin/ {
    proxy_pass http://127.0.0.1:8989;   # ⚠ 末尾不要加 /
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

**关键陷阱**：`proxy_pass` 末尾**不能写 `/`**。写成 `proxy_pass http://127.0.0.1:8989/;` 会把 `/api/admin/` 截断，导致后端 404。保留原路径，后端自身的 `/api/admin` 前缀才能匹配。

---

## 5. 上线前安全检查清单（必做）

| 位置 | 现状（占位/弱） | 处理 |
|---|---|---|
| 后端 `.env` `JWT_SECRET` | `change-me-before-production` | 改随机长串 `openssl rand -hex 32` |
| 后端 `.env` `ADMIN_PASSWORD` | `admin123456` | 改强口令 |
| 前端 `admin/src/configs/env/prod.js` `STORE_SECURE_KEY` | `please-replace-me-with-your-own-key` | 替换（注意：前端值会暴露给浏览器，只作本地存储加密用途，不要放真正密钥） |
| 后端端口 `8989` | 监听 `0.0.0.0` | 云防火墙关闭公网入站，仅 Nginx 本机反代 |

---

## 6. 验证

**后端**：
```bash
curl http://127.0.0.1:8989/api/admin/health        # 视实际路由调整
# 或测试登录接口
```
**前端**：浏览器打开 `https://dspadmin.wangwei.tech`，能进入登录页，用修改后的管理员账号登录成功。

**联调**：在前端执行一次需要 AI 的操作（如账号诊断），确认 `dspapi.wangwei.tech/api/admin/...` 链路通畅、StepFun 调用正常。

---

## 7. 日常更新部署（后续改了内容怎么发）

### 改了后端（Go）
```powershell
# 本地重新交叉编译
$env:GOOS="linux"; $env:GOARCH="amd64"; $env:CGO_ENABLED="0"
go build -o app ./cmd/main.go
```
1. 上传新 `app` 覆盖 `/www/wwwroot/dspapi/app`；
2. 重启进程（Supervisor 点重启，或 `kill` 旧进程后重新 `nohup ./app &`）；
3. 查看 `app.log` 确认启动无错（含自动迁移/种子日志）。

### 改了前端（Vue）
```bash
cd admin && pnpm install && pnpm build
```
1. 上传新的 `dist/` 覆盖 `/www/wwwroot/dspadmin/dist`；
2. 无需重启服务，刷新浏览器即可（建议清缓存/带版本号）。

### 改了 `.env`
改完重启后端进程使配置生效。

---

## 8. 排错速查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 前端能开但接口 404 | Nginx 反代 `proxy_pass` 末尾多了 `/` | 去掉末尾 `/`，保留原路径 |
| 前端报 CORS / 网络错误 | 反代未转发或域名解析未生效 | 检查 dspapi 解析、Nginx 配置、SSL |
| 后端启动即退出 | MySQL 连不上 / 库不存在 | 核对 `.env` 的 DB_*，确认库 `cy11_dsphk` 已建 |
| AI 功能报错 | `STEP_API_KEY` 为空或无效 | 填真实 key，检查 `STEP_BASE_URL` |
| 登录失败 | 改了 `ADMIN_PASSWORD` 但用了旧密码 | 核对 `.env` 并重启后端 |
